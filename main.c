#include <stdio.h>

char memory[4096];

int main() {
  // intial stuff to do:
  // 1. load rom
  // 2. loop through every 2 bytes
  // 3. print opcodes in right order
  // get window open with raylib maybe
  printf("hellow!\n");
  return 0;
}
